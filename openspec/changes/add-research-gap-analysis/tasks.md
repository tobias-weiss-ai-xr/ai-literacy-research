# Tasks — Add Research Gap Analysis

> Spec: `openspec/changes/add-research-gap-analysis/specs/research-gap-analysis/spec.md`
> Design: `openspec/changes/add-research-gap-analysis/design.md`

## 1. Tool implementation

- [x] 1.1 Create `tools/research_gap_analyzer.py` with a `main()` that reads
      `statistics.json`, `papers.yaml`, and `config/taxonomy.yaml`
- [x] 1.2 Implement the gap score: `thinness_weight * (1 - count/cat_avg) +
      momentum_weight * norm_12m_growth + floor_bonus if count <= gap_floor`
- [x] 1.3 Add CLI flags: `--top` (default 20), `--thinness-weight` (0.6),
      `--momentum-weight` (0.4), `--gap-floor` (5), `--write-doc`
- [x] 1.4 Normalize the score to 0–100 and sort descending
- [x] 1.5 Render `docs/research/gap_analysis.md`: summary table + per-cell
      sections with up to 5 representative papers (newest first)
- [x] 1.6 Add a footer: "Generated by tools/research_gap_analyzer.py — do not
      edit by hand."
- [x] 1.7 Ensure deterministic output (no timestamps in the body; stable key
      ordering) so byte-identical regeneration is possible

## 2. Config & display integration

- [x] 2.1 Use `research_config.category_display()` / `subcategory_display()`
      for human-readable names in the report (no raw kebab ids in headers)
- [x] 2.2 ~~momentum from `trend_keywords`~~ — **resolved in design**: the gap
      score's momentum term is *12-month publication growth per cell* (per
      `design.md` §Gap score formula), so `trend_keywords` are not consumed
      by the analyzer (still available via `scripts/research_config.py`).
- [x] 2.3 Handle empty/missing `statistics.json` gracefully (exit with a
      clear message: run `scripts/standard_stats.py` first)

## 3. Pipeline & CI integration

- [x] 3.1 Insert `tools/research_gap_analyzer.py` into the documented pipeline
      order: validate → readme → stats → **gap analysis** → reports → visualize
- [x] 3.2 Update `AGENTS.md`:
  - [x] 3.2.1 Add "What are the research gaps?" task → `python3 tools/research_gap_analyzer.py`
  - [x] 3.2.2 Add the tool to the full-pipeline command in rule 4
  - [x] 3.2.3 Add `docs/research/gap_analysis.md` to the read-only generated-files list (rule 2)
- [x] 3.3 Update `.github/workflows/validate.yml`:
  - [x] 3.3.1 Run `tools/research_gap_analyzer.py` in the regeneration step
  - [x] 3.3.2 Add `docs/research/gap_analysis.md` to the path triggers
- [x] 3.4 Update `README.md` pipeline section to list the gap analyzer step

## 4. Spec & contract updates

- [x] 4.1 Add `research-gap-analysis` capability to
      `openspec/specs/analysis-reporting/spec.md` (this change's delta spec is
      the source; on archive, sync the ADDED requirements into the main spec)
- [x] 4.2 Confirm `openspec/specs/paper-corpus/spec.md` already covers the
      read-only-artifact contract (it does); extend the generated-files list
      in AGENTS.md to name `docs/research/gap_analysis.md`

## 5. Verification

- [x] 5.1 Run the full pipeline on the current corpus and confirm
      `docs/research/gap_analysis.md` is generated
- [x] 5.2 Run the analyzer twice and confirm byte-identical output
- [x] 5.3 Manually inspect the top 5 gap cells against the README's
      "Research Gaps" list — thin-and-surging cells should rank highest
- [x] 5.4 `python3 scripts/validate_papers.py` exits 0
- [ ] 5.5 ~~`python3 scripts/generate_readme.py --check` exits 0~~ — **blocked, pre-existing**: the curated README is drift-tolerant (corpus grew 9,653→11,640; `refresh_readme_stats.py` is the repo's README keeper to avoid reverting curation). `--check` fails on `main` before this change too. Full regen intentionally not forced.
- [x] 5.6 Commit the generated `docs/research/gap_analysis.md` and the new tool
- [x] 5.7 Push to `main`; CI `validate` (incl. gap-report freshness check) passes.
